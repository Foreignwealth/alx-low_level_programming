#include "main.h"

/**
 * _isupper - Checkes upper case
 * @c : print int
 * Return: 0 and 1 success
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
}

else
	{
		return (0);
	}
}
