#include <stdio.h>
/**
 * main - Print a given output
 * created by opeyemi oladoyinbo
 * Return: Always 0 (Success)
 */

int main(void)
{
	char *s = "_putchar\n";

	while (*s)
		putchar(*s++);

	return (0);
}
